export const selectIsModalOpen = state => state.filters.isModalOpen;

export const selectIsLibraryModalOpen = state =>
  state.filters.isLibraryModalOpen;

export const selectBookTitle = state => state.filters.bookTitle;

export const selectBookAuthor = state => state.filters.author;
