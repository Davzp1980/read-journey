export const selectRecomendedBooks = state => state.books.recomended;

export const selectLibrary = state => state.books.library;

export const selectCurrentBook = state => state.books.book;

export const selectFavoriteBooks = state => state.books.favorites;
